import React from 'react';
import styled, { css } from 'styled-components';

let BoxShadow = css`
  box-shadow: 0px 5px 4px 2px rgba(0,0,0,0.2), 0px 7px 9px 5px rgba(0,0,0,.19);
`;

export default BoxShadow;